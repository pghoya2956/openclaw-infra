# ===== Infrastructure (infra/.env) =====

# AWS
AWS_VPC_ID=vpc-xxxxxxxxx
AWS_SUBNET_ID=subnet-xxxxxxxxx
AWS_KEY_NAME=MyKeyPair
AWS_HOSTED_ZONE_ID=Zxxxxxxxxx
BASE_DOMAIN=example.com

# HTTPS (Let's Encrypt)
ACME_EMAIL=you@example.com

# EC2 Tags (required by IAM policy)
EC2_TAG_OWNER=YourName
# EC2_TAG_PURPOSE=OpenClaw AI Assistant        # optional, default shown
# EC2_TAG_ENVIRONMENT=production               # optional, default shown
# EC2_TAG_EXPIRY=2026-12-31                    # optional

# SSH
# SSH_KEY_PATH=~/.ssh/id_ed25519              # optional, default shown

# Active agents (comma-separated, default: all)
# ENABLED_AGENTS=lab,ceo-advisor

# ===== Secrets (infra/.env.secrets) =====
# 통합 EC2 인스턴스에 주입되는 시크릿

# OPENCLAW_GATEWAY_TOKEN=<generate with: openssl rand -hex 32>
# ANTHROPIC_SETUP_TOKEN=sk-ant-oat01-xxx  (generate with: claude setup-token)

# Slack 계정별 토큰 — agent.yml의 slackAccount 대문자 변환
# SLACK_BOT_TOKEN__{ACCOUNT}=xoxb-xxx
# SLACK_APP_TOKEN__{ACCOUNT}=xapp-xxx
# 예: SLACK_BOT_TOKEN__CEO=xoxb-..., SLACK_APP_TOKEN__CEO=xapp-...
